{
  "name": "wangeditor5-for-vue3-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build"
  },
  "devDependencies": {
    "@microsoft/api-extractor": "^7.30.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  }
}
