{
  "name": "wangeditor5-for-vue3-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "run-s build:core build:shared build:docs build:elplus pages",
    "build:core": "pnpm --filter ./core build",
    "build:shared": "pnpm --filter ./shared build",
    "build:elplus": "pnpm --filter ./example/elplus build",
    "build:docs": "pnpm --filter ./docs build",
    "pages": "rimraf pages && gulp copyPages --require sucrase/register/ts -f gulpfile.ts",
    "publish": "pnpm publish --filter ./core --no-git-checks --dry-run"
  },
  "devDependencies": {
    "@microsoft/api-extractor": "^7.30.0",
    "@types/gulp": "^4.0.9",
    "gulp": "^4.0.2",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "sucrase": "^3.27.0"
  }
}
